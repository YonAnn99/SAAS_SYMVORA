// Motor de animacion de `motion` para <Movimiento>: se descarga APARTE y
// despues del primer pintado (import dinamico), no en el JS inicial.
import { domAnimation } from "motion/react";

export default domAnimation;
